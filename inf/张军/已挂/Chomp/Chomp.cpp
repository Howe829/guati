#include<bits/stdc++.h>
using namespace std;


int n,m;
int main()
{
    while(scanf("%d%d",&n,&m)!=EOF)
    {
        if(n==1&&m==1)
            cout<<"Yes"<<endl;
        else
            cout<<"No"<<endl;
    }
    return 0;
}
